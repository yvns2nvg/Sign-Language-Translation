# 세션 요약 (2026-09-04 ~ 09-07) — kslx: 누수 진단부터 문장 생성, 1200단어 확장까지

이 문서는 `add-kslx-webcam-verification` 브랜치에서 진행한 작업 전체를
한 번에 훑어볼 수 있게 정리한 요약이다. 세부 수치·재현 명령·한계는
[`kslx/README.md`](kslx/README.md)(사용법)와 [`kslx/RESULTS.md`](kslx/RESULTS.md)
(전체 실험 결과)에 있다.

## 0. 무엇을 하려고 시작했나

`model/`(기존 TCN, 3000클래스)이 val 99.6%를 보고했는데, 로컬에 남아있던
이전 세션 기록(`README.md`)이 이미 "이 숫자는 누수가 의심된다"고 적어뒀고,
그 증거를 잡을 유일한 방법인 **signer_out(미학습 수어자) 평가는 수어사가
1명뿐이라 불가능**하다고 못박아뒀다. 그런데 이 PC에는 그 사이 AI Hub 세션이
16명분 전부 받아져 있었다 — 프로젝트의 최대 병목이 이미 풀려 있는데
그걸 쓸 코드가 없는 상태였다. 여기서부터 시작했다.

## 1. 데이터 누수를 데이터만으로 증명

같은 take(수어사×단어)의 5개 카메라 앵글 파일에 든 **3D 키포인트가 비트
단위로 완전 동일**했다 (멀티뷰 삼각측량 결과 하나를 5개 파일에 복사해 넣은
것). 랜덤 분할에서는 val 클립의 **100%가 train에 같은 take(다른 앵글)를
가지고 있었다** — 이게 기존 99.6%의 정체다. 팀원이 별도로 만들어둔 3D npz
캐시(`DataSet/Dataset_NPZ`)로도 독립적으로 재확인했다: 같은 데이터에서
분할 방식만 바꿨더니 1-NN 정확도가 **72.0% → 0.0%**로 떨어졌다 — 순수하게
누수만으로 생기는 격차.

→ `kslx/` 는 이 문제를 원천 차단한다: **2D 키포인트만 사용**(`layout.py`),
**분할 프로토콜을 이름으로 명시**하고 조건이 안 맞으면 조용히 죽지 않고
에러를 던진다(`splits.py`).

## 2. 처음으로 나온 signer_out 수치

16명 수어사 × 271단어 × 5앵글(21,680클립)로 학습한 결과:

| 프로토콜 | top1 | 의미 |
|---|---|---|
| random | 99.5% | 참고용 — 100% 누수 |
| take_out | 92.4~93.6% | 같은 signer/단어 조합 미학습 |
| angle_out | 96.0~96.6% | 시점 일반화 상한선 |
| **signer_out** | **97.8~98.5%** | **미학습 수어자 — 이전엔 측정 불가였던 지표** |

과정에 진짜 버그도 하나 있었다: 정규화 코드가 어깨 동시 미검출 프레임에서
좌표를 최대 100만 배로 폭발시켜서, 고치기 전까지 모든 프로토콜이 순수
확률(1/271) 수준에 멈춰 있었다. (`kslx/normalize.py`, RESULTS.md §3)

## 3. 강건성 — 그리고 두 번째 버그 같은 발견

깨끗한 데이터로는 안 보이던 문제가 강건성 평가(`eval_robust.py`)에서
드러났다: **손이 잠깐 안 보이면 네 모델 다 정확도가 14%대로 붕괴**했다.
학습에 증강이 전혀 없었기 때문이다. 배치 단위 증강(회전/전단/스케일/
노이즈/손 결측, `kslx/augment.py`)을 넣어 재학습하니 **손 결측 98.8%,
yaw 30도 회전 99.2%**로 사실상 완전히 회복됐다 (깨끗한 데이터 손실은
1%p 안팎). **최종 추천 체크포인트: `runs/signer_out_aug.pt`** — 이미
저장소에 커밋돼 있어 클론 후 바로 쓸 수 있다.

## 4. 노트북 웹캠으로 실시간 검증

학습 데이터(AI Hub 5카메라, OpenPose 포맷)와 웹캠(MediaPipe)의 키포인트
체계가 달라 변환 어댑터(`kslx/adapters/mediapipe_adapter.py`)를 새로
만들었다 — pose/hand는 관절 대응이 확실하고, 얼굴 34점은 MediaPipe 공식
얼굴 영역 상수에서 뽑은 근사치(미검증, 이상하면 `--no-face`로 비교).

처음엔 SPACE를 눌러야 녹화가 시작/종료됐는데, 손 움직임 에너지로 "단어
끝 → 다음 단어 시작"을 자동 감지하는 게이트(`kslx/stream.py`)를 추가해서
이어서 인식할 수 있게 했다.

```bash
git clone -b add-kslx-webcam-verification https://github.com/yvns2nvg/Sign-Language-Translation.git
cd Sign-Language-Translation
pip install torch opencv-python mediapipe pillow numpy kiwipiepy
python -m kslx.realtime --ckpt runs/signer_out_c1200_aug.pt
```

## 5. 단어 나열 → 자연스러운 문장

수어로 어떻게 문장을 만드는지부터 조사했다. **한국수어(KSL)는 한국어를
그대로 손으로 옮긴 게 아니라 독립적인 문법을 가진 언어다**: 한국어(교착어)
와 달리 조사가 거의 없고(주격/목적격/관형격 조사 없음), 기본 SOV 어순이지만
더 자유롭고, 의문문 같은 문장 종류는 단어가 아니라 표정 같은 비수지
신호로 표시된다. 즉 인식 결과는 "조사·어미 없는 핵심어 나열"이고, 문장으로
바꾸는 건 기계번역의 gloss-to-text 문제다.

AI Hub 데이터엔 완성 문장 텍스트가 없어 학습 기반 접근은 못 쓰고, 사용자가
오프라인·무료를 선택해서 `kiwipiepy`(오프라인 한국어 형태소 분석기) 기반
규칙으로 구현했다(`kslx/sentence.py`). 인식된 단어를 모았다가 한동안 손이
멈추면 자동으로 문장을 완성한다:

| 인식된 단어 | 완성된 문장 |
|---|---|
| 나, 학교, 가다 | 나는 학교에 가요 |
| 음료수, 마시다 | 음료수를 마셔요 |
| 나, 슬프다 | 내가 슬퍼요 |
| 오늘, 날씨, 좋다 | 오늘 날씨가 좋아요 |

완벽하진 않다 — 연속된 명사 두 개(복합명사 vs 별개 성분)나 동사별 정확한
격틀까지는 구분 못 한다. 한계는 RESULTS.md §8에 정리했다.

## 6. 웹캠 실측: 단어 인식이 기대만큼 안 나온다는 피드백

노트북에서 실제로 테스트해보니 signer_out 97.8~98.5%만큼 단어 인식이
잘 안 된다는 피드백을 받았다. 조사해보니 **"OpenPose를 MediaPipe로
단순 대체하면 인식 성능이 크게 떨어진다"는 게 수어인식 연구에 이미
보고된 현상**이었다(두 검출기의 실패 패턴이 근본적으로 다름 — MediaPipe는
손 전체가 통째로 빠지고, OpenPose는 개별 관절만 빠진다). 즉 어느 정도는
구조적으로 예상된 문제지만, 코드에 실제 버그도 하나 더 있었다: 실시간
정규화 코드가 스트림 시작 직후(어깨너비를 아직 못 잰 시점)에 §2와 똑같은
"좌표 100만 배 폭발" 버그를 갖고 있어서 고쳤다. 추가로 MediaPipe 특유의
키포인트 흔들림을 줄이는 One-Euro 스무딩 필터와 시작 시 보정 단계를
넣었다. 근본적으로 더 끌어올리려면 웹캠 샘플을 직접 몇 개 녹화해서
분류기 마지막 레이어를 파인튜닝하는 게 문헌상 가장 효과적인 다음
단계다(`kslx/RESULTS.md` §9).

## 7. 271 → 1200단어 확장 (동사 부족 해결)

사용자가 "배우다"라는 수어가 있는지 물어서 확인해보니 3000단어 전체를
뒤져도 없었고, 더 찾아보니 271단어 안엔 동사·형용사가 7개뿐이었다(대부분
명사). AI Hub 3000단어 전체가 이미 디스크에 있어서 새로 녹화할 필요
없이 범위만 넓히면 됐는데, 여기서 이번 세션의 두 번째 큰 인프라 문제가
나왔다.

**3000단어 전체(240,000클립, ~20GB)는 이 PC(RAM 31GB)에 너무 컸다.**
메모리맵으로 처리해도 두 번이나 시스템에 의해 강제종료됐다 — 한 번은
빌드 중(20GB 파일에 쓰기 중 더티 페이지 누적), 한 번은 학습 중(검증셋을
뒤섞인 순서로 한 번에 읽어 파일 전체를 무작위로 훑는 것과 같아짐).
`kslx/data/build_dataset.py`(주기적 flush로 순차 쓰기)와
`kslx/train.py`/`kslx/eval_robust.py`(정렬 후 청크 단위로만 읽기,
`--max-classes`로 필요한 만큼만 잘라 쓰기)를 고쳐서, 실측으로 안전한
크기를 확인한 뒤 **1200단어(96,000클립, ~8.7GB)**로 재학습했다.

결과: **signer_out top-1 98.0%** — 271단어(97.8%)와 사실상 동일하다.
**어휘를 4.4배 늘려도 정확도가 떨어지지 않았다.** 강건성도 그대로
유지됐다(손 결측 -1.4%p). 동사·형용사가 121개로 늘어(가다/좋다/오다/
아프다/예쁘다 등) 훨씬 다양한 문장을 만들 수 있다:

| 인식된 단어 | 완성된 문장 |
|---|---|
| 나, 아프다 | 내가 아파요 |
| 나, 가다 | 나는 가요 |
| 나, 예쁘다 | 내가 예뻐요 |

**새 기본 추천 체크포인트: `runs/signer_out_c1200_aug.pt`**
(`kslx/VOCAB_1200.txt`). 3000단어 전체는 이 PC RAM으로는 여전히
위험해서 보류했다 — 더 큰 RAM이나 shard 기반 학습 루프가 있으면
다시 시도해볼 만하다. 자세한 원인 분석은 `RESULTS.md` §10.

## 8. 지금 상태 / 다음에 할 일

- ✅ 데이터 누수 데이터만으로 증명 (`leak_report.py`, `leak_report_npz.py`)
- ✅ signer_out 97.8~98.5% 확보 (이전엔 불가능했던 지표)
- ✅ 강건성 확보 (증강, 손 결측/시점변화 문제 해결)
- ✅ 노트북 웹캠 실시간 검증 (자동 단어 분할 포함)
- ✅ 단어 → 문장 규칙 기반 생성
- ✅ 웹캠 실시간 정규화 버그 수정 + 지터 완화 필터 + 시작 보정 단계
- ✅ 271 → 1200단어 확장 (동사 121개 포함, 정확도 유지)
- ⬜ 웹캠 재테스트로 개선 여부 확인 — **다음 피드백 대기 중**
- ⬜ 웹캠 샘플 녹화 + 분류기 파인튜닝 (문헌상 가장 효과적인 다음 단계)
- ⬜ MediaPipe ↔ AI Hub 얼굴 매핑 실측 검증 (원본 영상으로)
- ⬜ 3000단어 전체 확장 (더 큰 RAM 또는 shard 학습 루프 필요)
- ⬜ 문장(SEN) 데이터 학습, 학습된 background 클래스 기반 정식 경계 검출기
- ⬜ 문장 종류(의문문 등) — 얼굴 비수지 신호 인식 추가

전체 재현 명령과 상세 수치는 [`kslx/RESULTS.md`](kslx/RESULTS.md), 사용법은
[`kslx/README.md`](kslx/README.md) 참고.
